#include <stdio.h>
#include <stdlib.h>

struct queue{
    int head, tail, size;
    unsigned capacity;
    int *array;
};

struct queue* createCola(int capacity)
{
    
}

int main ()
{
    struct queue* clientes = crearCola(20);
    return 0;
}